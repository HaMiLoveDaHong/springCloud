eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/
  instance:
    hostname: localhost
server:
  port: 1001
spring:
  application:
    name: eureka-server
